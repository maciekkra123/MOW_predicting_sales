library(caret)
# Data_all <- read.csv("../Data_raw/Dataset_old.csv", na.strings="NaN")
# View(Data_all) #wglad w dane (widok tabelaryczny)

test_kaggle <- read.csv("Kaggle_data/TestDataset.csv")
View(test_kaggle)
